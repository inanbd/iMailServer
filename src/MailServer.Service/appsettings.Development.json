{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug"
    }
  },

  "MailServer": {
    "Server": {
      "Hostname": "mail.localhost.test"
    },

    "Security": {
      "//": "Development only. DPAPI is Windows-only, so a non-Windows development machine needs the file-backed protector. The options validator REFUSES this value when the environment is Production.",
      "SecretProtection": "Development"
    },

    "Database": {
      "Provider": "Sqlite",
      "Sqlite": {
        "DataSource": "Data/mailserver-dev.db"
      }
    }
  }
}
